#include <bits/stdc++.h>

using namespace std;

int main()
{
   string s1, s2;
  cin>>s1>>s2;
  int ind=0;
  int n= s1.size();
  int n2= s2.size();
  while(s1.find(s2)!=string::npos)
  {
  	int ind = s1.find(s2);
  	s1.erase(ind,n2);
  	//cout<<s1<<endl;
  }
  cout<<s1;
  return 0;
}
